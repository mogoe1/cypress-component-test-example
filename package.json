{
  "name": "cypress-component-test-example",
  "scripts": {
    "test": "npm run cypress:run",
    "cypress:run": "cypress run --component",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^10.3.0",
    "express": "^4.18.1"
  }
}